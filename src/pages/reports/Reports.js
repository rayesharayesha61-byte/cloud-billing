import React from "react";
import { Box, Typography } from "@mui/material";

const Reports = () => (
  <Box>
    <Typography variant="h4">Reports</Typography>
    <Typography color="text.secondary">
      Business reports will be available here.
    </Typography>
  </Box>
);

export default Reports;