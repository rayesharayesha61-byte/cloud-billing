import React from "react";
import { Box, Typography } from "@mui/material";

const Expenses = () => (
  <Box>
    <Typography variant="h4">Expenses</Typography>
    <Typography color="text.secondary">
      Expense management will be available here.
    </Typography>
  </Box>
);

export default Expenses;